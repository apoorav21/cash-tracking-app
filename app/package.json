{
  "name": "cashflow-app",
  "version": "1.0.0",
  "private": true,
  "description": "Cashflow tracker — Capacitor app (Android-first, also iOS & web)",
  "scripts": {
    "sync": "cap sync",
    "android": "cap run android",
    "open:android": "cap open android",
    "assets": "capacitor-assets generate --android",
    "serve": "npx --yes serve www"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/splash-screen": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
